<div class="col-md-4 mx-auto">

    <div class="card">
        <div class="card-body">
            <form action="/inventory/edit/{{link.id}}" method="POST">
                <div class="form-group">
                    <input type="text" name="title" class="form-control" placeholder="Title" value="{{link.title}}" autofocus>
                </div>
                <div class="form-group">
                    <input type="number" name="precio" class="form-control" placeholder="Precio" value="{{link.precio}}">
                </div>
                <div class="form-group">
                    <textarea name="description" rows="2" class="form-control" placeholder="Description">{{link.description}}</textarea>
                </div>
                <div class="form-group">

                       <div class="form-group">
                    <input type="number" name="almacen" class="form-control" placeholder="Almacén" value="{{link.almacen}}" autofocus>
                </div>
                <div class="form-group">
                    <input type="number" name="vendidos" class="form-control" placeholder="Vendidos" value="{{link.vendidos}}">
                </div>
                <div class="form-group">
                    <textarea name="margenbruto" rows="2" class="form-control" placeholder="Margen bruto">{{link.margenbruto}}</textarea>
                </div>
                <div class="form-group">


                    <button class="btn btn-success btn-block">
                        Save
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>